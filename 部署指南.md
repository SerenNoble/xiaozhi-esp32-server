# 自构建部署指南（含自定义改动）

> 本文件用于：在新机器上 `git clone` 本仓库后，按此文档一键起服务。
> 对应 compose：仓库根 `docker-compose-selfbuild.yml`
> 与官方 `main/xiaozhi-server/docker-compose_all.yml` 的区别：server/web 一律**本地构建**（自定义代码烤进镜像），不依赖任何主机路径或 volume 挂载源码。

---

## 0. 这个部署包含什么自定义改动

- **aipet 记忆 provider**：智控台 Memory 下拉可见（FTS5 本地记忆）
- **qwen / 豆包 缓存开关**：LLM 模型编辑页显示「是否开启缓存」开关（boolean）
- **doubao_cache provider**：豆包 Responses API Session 缓存
- **server 端核心定制**：当天日程注入（`connection.py`）+ xiaozhi-memory FTS5 检索修复（分词 / 字符串相似度 / 整句天花板）
- 以上全部已提交进 git，`git clone` 即含。

---

## 1. 前置条件

- Docker + Docker Compose（compose v2 插件）
- **国内机器**：`/etc/docker/daemon.json` 配置 registry-mirrors（拉 Docker Hub 的 node / maven / jre / python）：

  ```json
  {
    "registry-mirrors": ["https://docker.1ms.run"]
  }
  ```
  配完 `sudo systemctl restart docker`。
- mysql / redis 走 `docker.1ms.run` 镜像（compose 里已写死，无需额外配置）。
- server 镜像的 base 走 `ghcr.nju.edu.cn`（NJU 镜像，compose/Dockerfile 里已写死）。

---

## 2. 首次部署：准备运行时文件

compose 用 bind mount 挂载运行时数据，**首次启动前必须准备**（这些不入镜像、不入 git）。

> 如果是从旧机迁移，以下所有文件/目录均可从旧机对应路径直接拷贝，确保完整覆盖。

```bash
cd xiaozhi-esp32-server   # 仓库根

# ============================================
# (1) data/ — 配置文件 + 记忆数据库
# ============================================
mkdir -p data
# 必需文件（缺失任意一个 server 都会启动失败或功能异常）：
#   data/.config.yaml          — server 主配置（含 manager-api 地址/密钥）
#   data/.wakeup_words.yaml    — 唤醒词配置
#   data/xiaozhi_memory.db     — FTS5 本地记忆数据库

# 示例：从旧机拷贝
# cp /旧机/xiaozhi-esp32-server/data/.config.yaml       data/
# cp /旧机/xiaozhi-esp32-server/data/.wakeup_words.yaml data/
# cp /旧机/xiaozhi-esp32-server/data/xiaozhi_memory.db  data/

# ============================================
# (2) models/ — AI 模型文件
# ============================================
mkdir -p models/SenseVoiceSmall
mkdir -p models/snakers4_silero-vad

# SenseVoiceSmall/model.pt（ASR 语音识别模型，约 893MB）
#   ⚠️ 必须是真实文件，不能是目录！否则 compose 启动报 mount 错误
# cp /旧机/xiaozhi-esp32-server/models/SenseVoiceSmall/model.pt models/SenseVoiceSmall/

# snakers4_silero-vad/（VAD 语音活动检测 ONNX 模型，约 7MB）
#   包含 src/silero_vad/data/silero_vad.onnx 等
# cp -a /旧机/xiaozhi-esp32-server/models/snakers4_silero-vad/ models/

# ============================================
# (3) mysql/data/ — 数据库（如果从旧机迁移）
# ============================================
# 如果已有运行中的数据库，把旧数据目录拷贝过来：
# cp -a /旧机/xiaozhi-esp32-server/mysql/data/ mysql/
```

> `data/.config.yaml` 里的 `manager-api.secret` 必须与数据库中 `sys_params.server.secret` **完全一致**。首次部署时 web 容器会自动生成并写入数据库，再手动同步到 `.config.yaml` 中。

---

## 3. 一键启动

```bash
docker compose -f docker-compose-selfbuild.yml up -d --build
```

首次会构建两个镜像（耗时主要在拉 base + 装依赖，约 10~20 分钟）：
- `xiaozhi-esp32-server:server_aipet`（~10GB，含全部自定义 Python）
- `xiaozhi-esp32-server:web_aipet`（~570MB，含 aipet/缓存 changelog）

后续启动（代码没变）去掉 `--build` 即可秒起。

> **DNS 说明**：compose 已为 server 容器配置 `dns: [127.0.0.11, 114.114.114.114]`，确保同时能解析容器间通信（内嵌 DNS）和公网 API 域名（如讯飞 ASR、豆包 LLM）。**不要删这行，否则设备 ws 能连但不能对话**。

---

## 4. 验证

```bash
# 容器状态（4 个应全是 Up，RestartCount 应为 0）
docker compose -f docker-compose-selfbuild.yml ps

# 智控台（manager-web）
浏览器打开 http://<服务器IP>:8002

# ws 服务端口（对外 8001，容器内 8000）
# 视觉 http（8003）
```

**智控台验证**：
- 「Memory 类型」下拉应出现 **aipet**
- 编辑 qwen（openai 类型）/ 豆包模型，应看到「**是否开启缓存**」开关
- LLM「新增模型」下拉应出现「豆包(Session缓存)」

**server 启动验证**（关键！）：
```bash
# 确认无 DNS 解析错误（外网 API 可达）
docker logs xiaozhi-esp32-server 2>&1 | grep -c "Temporary failure in name resolution"
# 应输出 0

# 确认核心组件初始化成功
docker logs xiaozhi-esp32-server 2>&1 | grep "初始化组件"
# 应看到：vad成功、asr成功 等

# 确认 API 配置拉取成功
docker logs xiaozhi-esp32-server 2>&1 | grep "从API读取配置"
# 应有输出（不应有 "无效的服务器密钥"）

# 确认 ws 地址
docker logs xiaozhi-esp32-server 2>&1 | grep "Websocket地址"
```

**secret 验证**（如果从旧机迁移了数据库）：
```bash
# .config.yaml 中的 secret 必须与数据库 server.secret 一致
# 查数据库：
docker exec -e MYSQL_PWD=123456 xiaozhi-esp32-server-db \
  mysql -uroot -N xiaozhi_esp32_server -e "SELECT param_value FROM sys_params WHERE param_code='server.secret';"

# 对比 .config.yaml 中 manager-api.secret 是否相同
grep 'secret:' data/.config.yaml

# 如果二者一致但 server 仍报 "无效的服务器密钥"，检查 Redis 缓存：
docker exec xiaozhi-esp32-server-redis redis-cli HGET "sys:params" "server.secret"
# 如果 Redis 中的值不对，删除缓存让 web 回源数据库：
docker exec xiaozhi-esp32-server-redis redis-cli HDEL "sys:params" "server.secret"
docker compose -f docker-compose-selfbuild.yml restart xiaozhi-esp32-server-web
```

> provider 列表是**数据库驱动**的（`ai_model_provider` 表），web 容器启动时 Liquibase 自动跑 changelog 注册 aipet + 缓存开关，无需手动操作。

---

## 5. 数据库

- 容器：`xiaozhi-esp32-server-db`
- root 密码：`123456`，库名：`xiaozhi_esp32_server`
- 查库：

  ```bash
  docker exec -e MYSQL_PWD=123456 xiaozhi-esp32-server-db \
    mysql -uroot --default-character-set=utf8mb4 xiaozhi_esp32_server -e "SELECT id,provider_code FROM ai_model_provider;"
  ```

---

## 6. 更新代码后重新部署

```bash
git pull
docker compose -f docker-compose-selfbuild.yml up -d --build
```

- 改了 **Python**（server 端）→ 重建 server 镜像（`--build` 会自动处理）
- 改了 **Java / Vue / changelog**（web 端）→ 重建 web 镜像
- 只改 `data/config.yaml` → 重启 server 容器即可：`docker compose -f docker-compose-selfbuild.yml restart xiaozhi-esp32-server`

---

## 7. 常见问题

### 7.1 build 报 `error from sender: ... mysql/data/... permission denied`
`.dockerignore` 已排除运行时数据目录。若仍报，检查仓库里是否混入了 `main/xiaozhi-server/mysql/data/` 之类的运行时数据，删掉或加入 `.dockerignore`。

### 7.2 web 容器反复 Restarting（255）/ `exec /start.sh: no such file or directory`
CRLF 行尾问题。`Dockerfile-web` 已有 `sed -i 's/\r$//' /start.sh` 修复，**不要删这行**。如果是从 Windows 拉的代码，确保这行在。

### 7.3 `docker compose` 报 `no configuration file`
必须 `-f docker-compose-selfbuild.yml` 显式指定文件名（非默认名）。

### 7.4 server-base 拉不到 / build 卡在 FROM
默认走 NJU 镜像 `ghcr.nju.edu.cn/xinnan-tech/xiaozhi-esp32-server:server-base`。海外或换源可用：
```bash
SERVER_BASE_IMAGE=ghcr.io/xinnan-tech/xiaozhi-esp32-server:server-base \
  docker compose -f docker-compose-selfbuild.yml build
```

### 7.5 豆包缓存 403 / 不生效
- **403 AccessDenied.CacheService** → 火山账号未开通「推理(缓存)定价」，去火山方舟控制台开通（账号级操作，代码改不了）。
- 开了仍不触发 Session 缓存 → `doubao_cache.py` 首轮走 chat/completions 不存 response_id，待修（见仓库记忆）。

### 7.6 `git status` 显示 1000+ 文件 modified
**别慌，几乎全是 CRLF 行尾噪声**（仓库无 `.gitattributes`、`autocrlf` 未设）。真实改动用：
```bash
git diff -w --numstat | awk 'NF==3'   # 空行 = 纯 CRLF，不计
```
**不要** `git add -A`，会提交海量 CRLF 噪声。只 `git add` 真实改动的文件。

### 7.7 设备能连 ws 但不能对话（server 日志报 `Temporary failure in name resolution`）

**根因**：server 容器 DNS 未正确配置，无法解析外网 API 域名（如讯飞 ASR `iat.cn-huabei-1.xf-yun.com`、豆包 LLM 等）。

**检查**：
```bash
# 看日志中是否有 DNS 错误
docker logs xiaozhi-esp32-server 2>&1 | grep "Temporary failure in name resolution"
```

**修复**：确保 compose 中 server 服务有公网 DNS 配置：
```yaml
dns:
  - 127.0.0.11          # Docker 内嵌 DNS（容器间通信）
  - 114.114.114.114     # 公网 DNS（外网 API）
```
**不要只配 `127.0.0.11`**，否则只能解析内部容器名，外网 API 全挂。

### 7.8 server 反复重启 + 日志 `无效的服务器密钥`

**根因**：`.config.yaml` 中的 `manager-api.secret` 与数据库/Redis 中的 `server.secret` 不一致。

**两种常见场景**：
- **场景 A（全新部署）**：先启动 web 容器，等 `initServerSecret()` 自动生成 secret 写入数据库，然后从智控台「参数管理」复制 `server.secret` 到 `data/.config.yaml` 的 `manager-api.secret`。
- **场景 B（从旧机迁移数据库）**：数据库已迁移但 Redis 仍缓存旧值，导致 web 读取错误 secret。

**修复场景 B**：
```bash
# 删除 Redis 中的旧缓存，让 web 回源数据库
docker exec xiaozhi-esp32-server-redis redis-cli HDEL "sys:params" "server.secret"
# 重启 web 使其重新加载
docker compose -f docker-compose-selfbuild.yml restart xiaozhi-esp32-server-web
# 重建 server
docker compose -f docker-compose-selfbuild.yml up -d xiaozhi-esp32-server
```

### 7.9 端口冲突（8000/8001 被占用）

如果有其他非本项目容器（如 `djangowechat_web`）占用了 ws 端口，server 容器会因端口分配失败而无法启动或没有端口映射。

```bash
# 查谁占了端口
ss -tlnp 'sport = :8001'

# 停止冲突容器（注意：不要误停其他项目服务）
docker stop <冲突容器名>

# 重建 server
docker compose -f docker-compose-selfbuild.yml up -d xiaozhi-esp32-server
```

### 7.10 server 启动报 `mount ... not a directory`

**根因**：`models/SenseVoiceSmall/model.pt` 在宿主机上是**目录**而非文件。

docker-compose 挂载时如果宿主机路径是目录但容器内同路径需要是文件，会报此错误。多见于从旧机拷贝 ASR 模型时路径层级错误。

```bash
# 检查：应是普通文件（不应显示 d 开头）
ls -la models/SenseVoiceSmall/
# -rw-r--r--  ...  model.pt   ← 正确：- 开头表示文件
# drwxr-xr-x  ...  model.pt   ← 错误：d 开头表示目录

# 修复：删除假目录，拷入真实模型文件
rm -rf models/SenseVoiceSmall/model.pt
cp /旧机路径/model.pt models/SenseVoiceSmall/model.pt
docker compose -f docker-compose-selfbuild.yml up -d xiaozhi-esp32-server
```

### 7.11 web 容器启动后智控台返回 502

server 端连不上 web 管理 API，常见原因：
- **Authorization header 被 nginx 吞掉**：本仓库 `docs/docker/nginx.conf` 已配置 `proxy_set_header Authorization $http_authorization;`，确保这行存在。
- **server DNS 无法解析容器名**：见 7.7，确保 compose 中有 `dns: 127.0.0.11`。

---

## 8. 端口 / 凭据速查

| 项 | 值 |
|---|---|
| 智控台 | `8002` |
| ws（设备连接，宿主机对外） | `8001` |
| ws（容器内） | `8000` |
| 视觉 http | `8003` |
| MySQL | 容器内 `3306`，root/`123456`，库 `xiaozhi_esp32_server` |
| Redis | 容器内 `6379`，无密码 |
| server 镜像 | `xiaozhi-esp32-server:server_aipet` |
| web 镜像 | `xiaozhi-esp32-server:web_aipet` |

> `server.websocket` 全局参数（数据库 `sys_params`）需与实际宿主机 ws 端口一致，初始为 `ws://<IP>:8001`。

---

## 9. 目录结构（运行后）

```
xiaozhi-esp32-server/          # 仓库根 = 部署目录
├── docker-compose-selfbuild.yml
├── 部署指南.md                 ← 本文件
├── Dockerfile-server / Dockerfile-web
├── data/                       # 配置文件 + 记忆库（运行时，gitignore）
│   ├── .config.yaml            #   server 主配置
│   ├── .wakeup_words.yaml      #   唤醒词
│   └── xiaozhi_memory.db       #   FTS5 本地记忆
├── models/                     # AI 模型（运行时，gitignore）
│   ├── SenseVoiceSmall/
│   │   └── model.pt            #   ASR 语音识别模型（~893MB）
│   └── snakers4_silero-vad/    #   VAD 语音活动检测
│       └── src/silero_vad/data/
│           ├── silero_vad.onnx
│           └── silero_vad.jit
├── mysql/data/                 # MySQL 数据（运行时，gitignore）
├── uploadfile/                 # 上传文件（运行时，gitignore）
└── main/                       # 源码
```
